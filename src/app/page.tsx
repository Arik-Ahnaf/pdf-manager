import { PageContainer } from "@/components/layout/page-container";
import { HomeWorkspace } from "@/components/tools/home-workspace";

export default function HomePage() {
  return (
    <PageContainer narrow>
      <HomeWorkspace />
    </PageContainer>
  );
}
